var userScore = 0;
var systemScore = 0;

var inputs = ["rock", "paper", "scissor"];

function playGame (user){
    var computer = inputs[Math.floor(Math.random()*3)]
    Math.floor(Math.random())
    console.log(inputs[Math.floor(Math.random()*3)]);
    document.getElementById("Feedback").src = "images/" + inputs[Math.floor(Math.random()*3)] + ".png";
    console.log("images/" + computer + ".png");

    if (clicked == computer){
        document.getElementById("").innerText = "uavgjort"
    }
    else
    if(clicked == "scissor" && computer){

    }